{
  "skill_name": "promote-knowledge",
  "fixture_note": "Fixture memory dir in evals/fixture-memory/ contains 6 PROMOTE nominations (gc log) spanning the filter spectrum. Runs are non-interactive: the proposal table is the deliverable and NO repo file may be edited. The dedup-path tests need a target repo whose docs already cover reference_gcs_sni and reference_bc_default_feature — stage one before running (any repo with those two facts planted in its docs works); reference_hls_endlist is fictional and must be absent from the target repo (pure promote-path test).",
  "evals": [
    {
      "id": 0,
      "eval_name": "sharing-filter",
      "prompt": "memory-gc has produced PROMOTE nominations — run the promote-knowledge pass. (Use the fixture as the memory dir and the staged target repo as the destination repo; non-interactive; stop after producing the proposal table; modify no files.)",
      "expected_output": "Proposal table: HLS ENDLIST promoted with a scoped destination; GCS SNI and storage-connector default feature dropped as already documented (with cited paths); VM swap and reviewer note rejected at the sharing filter; digest pinning's fact-vs-prescription question engaged explicitly; no file edits.",
      "files": ["fixture-memory/"],
      "assertions": [
        "Promotes reference_hls_endlist (environment-general, absent from repo docs) with a concrete scoped destination (rules file with paths: frontmatter, docs/, or README — not root CLAUDE.md)",
        "Drops reference_gcs_sni as already documented, citing the target repo's existing doc path for it",
        "Rejects lesson_vm_swap — personal-VM-specific fact, fails the environment-generality test",
        "Rejects project_reviewer_note — teammate availability/health, hard-banned category",
        "Engages the fact-vs-prescription rule for feedback_digest_pinning explicitly: either flags it as prescription needing team consensus, or justifies a fact classification via existing repo enforcement",
        "Drops or converts reference_bc_default_feature to no-op/update because the target repo already documents the default feature",
        "Modifies no repository file and no fixture file (non-interactive run stops at the table)"
      ]
    }
  ]
}
